// // Valida sí es directorio
// // --------------------------
// const isDirectory = (pathUser) => {
//     if(fs.lstatSync(pathUser).isDirectory()){
//       return true;
//     } 
//       return false;
//     };

// // Valida sí es archivo
// // -----------------------
// const validaFile = (pathUser) => {
//     if(fs.statSync(pathUser).isFile()) {
//       return true;
//     } 
//     return false;
//   };
//   const routes = [];
//   const getFiles = validaFile(pathUser);
//   if (file) routes.push (pathUser);
//   return routes;

//   const fullPath = path.join(---dirname , pathName);
//   fs.readdir (fullPath, (error , files))


// }

